<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Hardware ── -->
    <uses-feature android:name="android.hardware.type.watch" />

    <!-- ── Network (MQTT) ── -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ── Samsung Health Sensor SDK – body sensors ── -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <!-- Required on Wear OS 4 (API ≥ 33) for background measurement -->
    <uses-permission android:name="android.permission.BODY_SENSORS_BACKGROUND" />
    <!-- Required for step counter/health tracking on Wear OS -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- Skin temperature (Health Connect / Wear OS 4+) -->
    <uses-permission android:name="android.permission.health.READ_SKIN_TEMPERATURE" />
    <!-- Samsung skin temp / SpO2 on Wear OS 5+ (API 36+) -->
    <uses-permission android:name="com.samsung.android.hardware.sensormanager.permission.READ_ADDITIONAL_HEALTH_DATA" />

    <!-- ── Foreground service to keep MQTT + sensor alive ── -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- API 34+ requires declaring the type -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />

    <!-- Tell Samsung Health Sensor SDK which services we may bind to -->
    <queries>
        <package android:name="com.samsung.android.service.health" />
        <package android:name="com.sec.android.app.samsungapps" />
    </queries>

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application
        android:name=".WatchApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- VitalsMonitorService: HR, skin temperature, SpO₂ → MQTT (Galaxy Watch 8) -->
        <service
            android:name=".service.VitalsMonitorService"
            android:exported="false"
            android:foregroundServiceType="dataSync|health" />

    </application>
</manifest>
