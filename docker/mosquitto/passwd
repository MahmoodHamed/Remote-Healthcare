# Mosquitto password file
# Generate real passwords with: mosquitto_passwd -c passwd <username>
# Example entry (do NOT use in production - replace with real hash):
# rpm_server:$6$salt$hashedpassword
